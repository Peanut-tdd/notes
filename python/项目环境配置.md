## 项目环境配置

**如果不配置项目环境，pip install XXX组件就是全局安装**

排查方法

```
which pip

pip show xxx 
```





vscode新建项目创建虚拟环境命令

```
python -m venv .venv
source .venv/bin/activate
```



activateEnvironment 自动进入.venv